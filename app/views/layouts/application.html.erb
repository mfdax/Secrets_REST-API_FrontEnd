<!DOCTYPE html>
<html>
<head>
  <title>Secrets</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<% if alert %>
  <%= alert %>
<% end %>

<% if notice %>
  <%= notice %>
<% end %>

<% if session[:email] != nil %>
  The session[:email] IS.... <%= session[:email] %>
  <button class"btn btn-default"><%= link_to "Sign Out", sign_out_path, method: :delete %></button>
<% end %>

<div class="container">
  <%= yield %>
</div>

</body>
</html>
